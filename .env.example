# Copy this file to .env and fill in your values
# These environment variables will be loaded into the container

# Agent Configuration
AGENT_UID=1000
AGENT_GID=1000

# Environment Variables
NPM_CONFIG_PREFIX=/home/agent/npm
NODE_ENV=production
TZ=America/Toronto

# API Keys (optional - can be set during runtime)
GEMINI_API_KEY=
OPENCODE_API_KEY=
OPENROUTER_API_KEY=

# Container Settings
NODE_ENV=production
NPM_CONFIG_PREFIX=/home/agent/npm
